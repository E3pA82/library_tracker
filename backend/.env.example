# backend/.env.example
# ===========================================
# Copier ce fichier vers .env et remplir les valeurs
# cp .env.example .env (Linux/Mac)
# copy .env.example .env (Windows)
# ===========================================

# Clé secrète Django (générer avec: python -c "import secrets; print(secrets.token_urlsafe(50))")
SECRET_KEY=votre-cle-secrete-a-generer

# Configuration PostgreSQL
DB_NAME=library_db
DB_USER=postgres
DB_PASSWORD=votre-mot-de-passe-postgresql
DB_HOST=localhost
DB_PORT=5432